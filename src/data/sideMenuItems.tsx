
import PATHS from "./paths";
import PERMISSIONS from "./permissions";

const sideMenuItems = [
  {
    name: "menu.create_entry",
    path: PATHS.PATH_KEY,
    permissions: PERMISSIONS[PATHS.PATH_KEY],
  },
]

export default sideMenuItems;