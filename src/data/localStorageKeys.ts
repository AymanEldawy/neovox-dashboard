const prefixer = "CP_";

const LOCAL_STORAGE_KEYS = {
  USER_KEY: prefixer + "USER_KEY",
  TOKEN_KEY: prefixer + "TOKEN_KEY",
  THEME_KEY: prefixer + "THEME_KEY",
};

export default LOCAL_STORAGE_KEYS;
